#!/usr/bin/env python

import setuptools  # type:ignore

setuptools.setup(
    name="ppqm",
    version="0",
    maintainer="Jimmy Kromann",
    python_requires=">=3.6",
    install_requires=[],
    packages=["ppqm"],
)
